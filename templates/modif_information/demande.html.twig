{% extends 'baseEmp.html.twig' %}

{% block title %}Modification d'information{% endblock %}

{% block body %}
<style>
    .page-title {
        margin-bottom: 15px;
        font-weight: bold;
        color: #FF8C00;
        font-family: Times;
        border-bottom: 2px solid #FF8C00;
        padding-bottom: 10px;
        display: inline-block;
    }

    .form-container {
        background-color: white;
        border-radius: 8px;
        padding: 25px;
        box-shadow: 0 4px 15px rgba(0,0,0,0.1);
        margin-top: 25px;
    }

    .form-control-lg {
        padding: 10px 12px;
        border-radius: 8px;
        border: 1px solid #ced4da;
        transition: border-color 0.3s;
    }

    .form-control-lg:focus {
        border-color: #FF8C00;
        box-shadow: 0 0 0 0.25rem rgba(255, 140, 0, 0.25);
    }

    .btn-submit {
        background-color: #FF8C00;
        color: white;
        padding: 10px 20px;
        border: none;
        border-radius: 8px;
        font-weight: 600;
        transition: all 0.3s;
    }

    .btn-submit:hover {
        background-color: #e67e00;
        transform: translateY(-2px);
    }

    .form-label {
        font-weight: 500;
        color: #495057;
        margin-bottom: 6px;
    }

    .bg-glass {
        background-color: rgba(255, 255, 255, 0.9) !important;
        backdrop-filter: blur(10px);
    }
</style>

<div class="container mt-5">
    <div class="row justify-content-center">
        <div class="col-lg-8">
            <h1 class="page-title text-center">Modification d'information personnel</h1>

            <div class="card form-container bg-glass">
                <div class="card-body">
                    {{ form_start(for, {'attr': {'class': 'needs-validation', 'novalidate': 'novalidate'}}) }}

                    <div class="mb-4">
                        {{ form_row(for.TypeModification, {
                            'attr': {
                                'class': 'form-control form-control-lg'
                            }
                        }) }}
                    </div>

                    <div class="mb-4">
                        {{ form_row(for.libelle, {
                            'attr': {
                                'class': 'form-control form-control-lg'
                            }
                        }) }}
                    </div>

                    <div class="text-center mt-4">
                        <button type="submit" class="btn btn-submit btn-lg">
                            <i class="fas fa-save me-2"></i> Enregistrer
                        </button>
                    </div>

                    {{ form_end(for) }}
                </div>
            </div>
        </div>
    </div>
</div>
{% endblock %}
